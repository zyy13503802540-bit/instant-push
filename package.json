{
  "name": "instant-push",
  "version": "0.6.1",
  "private": true,
  "type": "module",
  "description": "SullyOS Instant Push — Cloudflare Worker (deploy-from-git entry).",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@rei-standard/amsg-instant": "^0.6.1"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
